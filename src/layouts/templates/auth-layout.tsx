import FullVh from "@modules/general/components/full-vh";
import React from "react";

const authLayout = ({ children }: { children: any }) => (
  <FullVh>{children}</FullVh>
);

export default authLayout;
